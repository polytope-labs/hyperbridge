[package]
name = "tendermint-verifier"
version = "0.1.0"
edition = "2021"
description = "CometBFT light client verifier for ISMP"
authors = ["Polytope Labs <hello@polytope.technology>"]
publish = false

[dependencies]
# Core dependencies
cometbft-light-client-verifier = { workspace = true, default-features = false }
cometbft = { workspace = true, default-features = false  }
cometbft-proto = { workspace = true, default-features = false }
thiserror = { version = "2.0.12", default-features = false }
serde = { version = "1.0", features = ["derive"], default-features = false }
prost = { workspace = true, default-features = false }
tendermint-primitives = { workspace = true, default-features = false }

[dependencies.polkadot-sdk]
workspace = true
features = ["sp-io", "sp-core"]
default-features = false

[features]
default = ["std"]
std = [
    "serde/std",
    "polkadot-sdk/std",
    "thiserror/std",
    "prost/std",
    "cometbft/std",
    "cometbft-proto/std",
    "tendermint-primitives/std",
]

 