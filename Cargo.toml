[workspace]
resolver = "2"
members = [
    "primitives",
    "consensus",
    "messaging",
    "parachain",
    "relayer",
    "integration-tests",
]

[profile.release]
panic = "unwind"
opt-level = 3
