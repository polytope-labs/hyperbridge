export interface TeleportParams {
    amount: number
    fee: number
    assetId: string
    redeem: boolean
    to: string
    feeToken: string
    dest: string
    timeout: number
    data: string
    amountInMax: number
}