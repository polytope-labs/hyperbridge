# ============================================
# Hyperbridge TRON Deployment — Testnet
# ============================================
# Copy to .env.testnet and fill in your values:
#   cp .env.testnet.example .env.testnet
#
# Used by: tronbox migrate --network shasta
#          tronbox migrate --network nile
#          tronbox migrate --network development

# Private key of the deployer account (hex, no 0x prefix)
PRIVATE_KEY=

# Admin address (base58 T-address)
ADMIN=

# Hyperbridge parachain ID
PARA_ID=4009

# Network type — controls the state machine identifier encoding
# testnet → KUSAMA-{PARA_ID}
NETWORK=testnet

# Fee token address on TRON testnet (TRC-20, base58)
# Leave empty to use address(0) for local development
FEE_TOKEN=

# Fee token decimals (6 for USDT/USDC)
FEE_TOKEN_DECIMALS=6

# SunSwap V2 Router address on TRON testnet
# Leave empty if fee token swaps are not needed
UNISWAP_V2=

# Hex-encoded initial BEEFY consensus state
# Leave empty to skip setConsensusState during deployment
CONSENSUS_STATE=

# ── IntentGatewayV2 Parameters ───────────────────────────

# Enable solver selection (true/false)
SOLVER_SELECTION=false

# Surplus share in basis points (10000 = 100%)
SURPLUS_SHARE_BPS=0

# Protocol fee in basis points (10000 = 100%)
PROTOCOL_FEE_BPS=0

# Price oracle address (leave empty to use address(0))
PRICE_ORACLE=
